---
description: Inicia o servidor backend do Limbus automaticamente
---
// turbo-all

## Iniciar Servidor Limbus

1. Iniciar o servidor backend:

```bash
powershell -ExecutionPolicy Bypass -Command "node server.js"
```
Use `c:\Users\Mais_40351\.gemini\antigravity\scratch\Limbus\backend` as the working directory.
